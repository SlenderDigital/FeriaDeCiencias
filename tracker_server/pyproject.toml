[project]
name = "mediapipe-py"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Ironowl1907", email = "facuguialtana7@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "mediapipe<=0.10.21",
]

[project.scripts]
mediapipe-py = "mediapipe_py:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
